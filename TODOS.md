
- [x] Expose CEF's browser-host audio mute API, if not already the case, so mods can later mute whole Chromium windows/tabs.
- [x] Uncap FPS to more than 30FPS, if not already the case. Target should be at least 60FPS, but maybe also up to 120FPS (should be 60 by default, but should have an API method or something to enable 120, if Chromium/CEF even supports 120FPS at all).
